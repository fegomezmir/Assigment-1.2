<%= form_with(model: promise, local: true) do |form| %>
  <% if promise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promise.errors.count, "error") %> prohibited this promise from being saved:</h2>

      <ul>
      <% promise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :promise_value %>
  </div>

  <div class="field">
    <%= form.label :Condition %>
    <%= form.text_field :Condition, id: :promise_Condition %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
